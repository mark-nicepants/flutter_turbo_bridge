name: benchmark_target_app
description: Minimal Flutter app used as benchmark target for Turbo Bridge
resolution: workspace
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  turbo_bridge:
    path: ../../packages/turbo_bridge

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
