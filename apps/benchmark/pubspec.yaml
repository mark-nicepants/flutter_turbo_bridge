name: flutter_turbo_bridge_benchmark
description: Speed benchmark for Flutter AI interaction approaches
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  vm_service: ^15.2.0
  web_socket_channel: ^3.0.0
  args: ^2.4.0
  path: ^1.8.0
  turbo_bridge_client:
    path: ../../packages/turbo_bridge_client

dev_dependencies:
  test: ^1.24.0
