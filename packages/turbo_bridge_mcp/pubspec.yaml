name: turbo_bridge_mcp
description: MCP server for Flutter Turbo Bridge — exposes Flutter app interaction tools to LLM hosts via the Model Context Protocol.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  args: ^2.4.0
  mcp_dart: ^2.1.1
  turbo_bridge_client:
    path: ../turbo_bridge_client

dev_dependencies:
  test: ^1.24.0
  mocktail: ^1.0.0
